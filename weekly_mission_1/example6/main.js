require('./patcher');//llamamos al modulo que modifica al objeto
const logger=require('./logger');//llamamos al modulo que modifica la clase

logger.customMessage();//llamamos a la funcion que agregamos al objeto')